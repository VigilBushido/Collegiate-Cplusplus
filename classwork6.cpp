Sergio Munguia : ml56
Morgan Roberts

#include <iostream>
#include <string>
#include <iomanip>
using namespace std;

//Define a structure 
struct Employee
{
	//properties
	string firstName, lastName, department;
	float salary;
	Employee()
	{	
		firstName = "firstname";
		lastName = "lastname";
		department = "dept";
		salary = 0.0;
	}
};

int main()
{
// declare an array in main
myEmployees = {4}
// declare an int variable in main
int numOver45 = 0;

//Prompt for 4 employees and store in the array myEmployees
	for (int i = 0; i <= 3; i++)
	{	
	myEmployees += Employee.GetEmployee();
	}

//calculates the number of salaries over 45000
SalariesOver45();
 

Employee GetEmployee();

	cout << "Enter the first name of employee: ";
	cin >> Employee.firstName;
	cout << "Enter the last name of employee: ";
	cin >> Employee.lastName;
	cout << "Enter the Department the employee works in: ";
	cin >> Employee.department;
	cout << "Enter the employee salary: ";
	cin >> Employee.salary;
	cout << endl;

//Create the following output:
void DisplayEmployee(Employee);

cout << left << setw(25) << "Name" << left << setw(15) << "Position" << "Salary" << endl;
cout << "---------------------------------------------------------------------------" << endl;
cout << Employee.name << Empolyee.position << Employee.salary << endl;



// Calculate the number of salaries over 45000
int SalariesOver45(const Employee[]. int);
	{
		for (int i = 0; i <= 3; i++)
			if (Employee.salary > 45000)
				cout++;
	}
	
	

